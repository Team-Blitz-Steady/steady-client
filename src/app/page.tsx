"use client";

import StickyButton from "@/components/_common/StickyButton";

const Home = () => {
  return (
    <main>
      <StickyButton onClick={() => console.log("Hi")} />
    </main>
  );
};

export default Home;
